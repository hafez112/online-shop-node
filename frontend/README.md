Frontend (React + Vite + Tailwind)

1) تثبيت:
   cd frontend
   npm install

2) تشغيل:
   npm run dev

ملاحظة: لضبط الاتصال مع الـ backend، يمكنك تعيين متغير VITE_API_BASE في ملف .env.local مثل:
VITE_API_BASE=http://localhost:4000/api
